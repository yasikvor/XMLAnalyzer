Author: Yaroslav Kis
libs:
    jsoup - for parsing HTML files.
How can you start the app:
    1. Build .jar file
    2. Choose .jar file-with-dependencies
    3. Run it in cmd with provided arguments(example: java -jar App.jar file1.html file2.html)
    4. Output result : full path to found node (example: html > body > div > div > div > a)