Author: Yaroslav Kis

libs:
    jsoup - for parsing HTML files.
How can you start the app:
    1. Run Executable.jar with provided arguments(example: java -jar Executable.jar file1.html file2.html)
    2. Output result : full path to found node (example: html > body > div > div > div > a)